[package]
name = "report-save"
version = "0.1.0"
edition = "2021"
license.workspace = true
repository.workspace = true
homepage.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
arrow = "58.2.0"
bytes = "1"
parquet = { version = "58.2.0", default-features = false, features = ["arrow", "simdutf8", "zstd"] }
tar = { version = "0.4.45", default-features = false }
serde.workspace = true
serde_json.workspace = true
dashboard.workspace = true
metriken-query.workspace = true
tracing = "0.1"
# Reader-only: report `.rez` assembly (trim + parquet ingest) must stay
# metriken-free so the WASM viewer can build a report in the browser. The
# binary unifies this to the full `write` build via its own dependency.
rez = { path = "../rez", default-features = false }
