# Standalone workspace root so xtask keeps its own lockfile and stays out of
# the main rezolus workspace build graph.
[workspace]

[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"

